meta {
  name: Get Workflow Note
  type: http
  seq: 22
}

get {
  url: {{baseURL}}/api/operations/workflows/{{workflowId}}/notes/{{workflowNoteId}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
}

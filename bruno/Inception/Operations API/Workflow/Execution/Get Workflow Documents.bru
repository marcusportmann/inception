meta {
  name: Get Workflow Documents
  type: http
  seq: 34
}

get {
  url: {{baseURL}}/api/operations/workflows/{{workflowId}}/documents
  body: none
  auth: inherit
}

vars:post-response {
  documentId: res.body.workflowDocumentSummaries[0].documentId
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}

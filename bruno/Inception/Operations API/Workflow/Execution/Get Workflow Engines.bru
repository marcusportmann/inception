meta {
  name: Get Workflow Engines
  type: http
  seq: 5
}

get {
  url: {{baseURL}}/api/operations/workflow-engines
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
}

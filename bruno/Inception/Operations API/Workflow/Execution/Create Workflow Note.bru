meta {
  name: Create Workflow Note
  type: http
  seq: 20
}

post {
  url: {{baseURL}}/api/operations/create-workflow-note
  body: json
  auth: inherit
}

body:json {
  {
    "workflowId": "{{workflowId}}",
    "content": "The test workflow note content."
  }
}

vars:post-response {
  workflowNoteId: res.body
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
}

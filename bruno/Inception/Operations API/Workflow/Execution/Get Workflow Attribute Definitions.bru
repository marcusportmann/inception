meta {
  name: Get Workflow Attribute Definitions
  type: http
  seq: 5
}

get {
  url: {{baseURL}}/api/operations/workflow-attribute-definitions
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
}

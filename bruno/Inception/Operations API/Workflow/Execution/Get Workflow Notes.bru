meta {
  name: Get Workflow Notes
  type: http
  seq: 26
}

get {
  url: {{baseURL}}/api/operations/workflows/{{workflowId}}/notes
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
}

meta {
  name: Get Workflow Notes
  type: http
  seq: 24
}

get {
  url: {{baseURL}}/api/operations/workflows/{{workflowId}}/notes
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}

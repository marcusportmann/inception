meta {
  name: Request Workflow Document (Another)
  type: http
  seq: 31
}

post {
  url: {{baseURL}}/api/operations/request-workflow-document
  body: json
  auth: inherit
}

body:json {
  {
    "workflowId": "{{workflowId}}",
    "documentDefinitionId": "another_test_document_definition"
  }
}

vars:post-response {
  anotherWorkflowDocumentId: res.body
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
}

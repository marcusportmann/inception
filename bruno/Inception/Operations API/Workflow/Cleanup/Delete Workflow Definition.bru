meta {
  name: Delete Workflow Definition
  type: http
  seq: 6
}

delete {
  url: {{baseURL}}/api/operations/workflow-definitions/{{workflowDefinitionId}}
  body: none
  auth: inherit
}

vars:pre-request {
  workflowDefinitionCategoryId: test_workflow_definition_category
  workflowDefinitionId: test_workflow_definition
}

assert {
  res.status: eq 204
}

settings {
  encodeUrl: true
}

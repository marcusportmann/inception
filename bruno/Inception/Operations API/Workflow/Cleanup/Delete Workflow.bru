meta {
  name: Delete Workflow
  type: http
  seq: 4
}

delete {
  url: {{baseURL}}/api/operations/workflows/{{workflowId}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 204
}

settings {
  encodeUrl: true
}

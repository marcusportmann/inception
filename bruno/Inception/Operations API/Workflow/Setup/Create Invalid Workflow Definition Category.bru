meta {
  name: Create Invalid Workflow Definition Category
  type: http
  seq: 11
}

post {
  url: {{baseURL}}/api/operations/workflow-definition-categories
  body: json
  auth: inherit
}

body:json {
  {
  }
}

assert {
  res.status: eq 400
}

settings {
  encodeUrl: true
}

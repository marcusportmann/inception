meta {
  name: Create Document Note
  type: http
  seq: 2
}

post {
  url: {{baseURL}}/api/operations/create-document-note
  body: json
  auth: inherit
}

body:json {
  {
    "documentId": "{{documentId}}",
    "content": "The test document note content."
  }
}

vars:post-response {
  documentNoteId: res.body
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
}

meta {
  name: Get Document Note
  type: http
  seq: 10
}

get {
  url: {{baseURL}}/api/operations/documents/{{documentId}}/notes/{{documentNoteId}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
}

meta {
  name: Finalize Workflow
  type: http
  seq: 17
}

post {
  url: {{baseURL}}/api/operations/finalize-workflow
  body: json
  auth: inherit
}

body:json {
  {
    "workflowId": "{{workflowId}}",
    "status": "completed"
  }
}

assert {
  res.status: eq 204
}

settings {
  encodeUrl: true
}

meta {
  name: Get Workflow
  type: http
  seq: 13
}

get {
  url: {{baseURL}}/api/operations/workflows/{{workflowId}}
  body: none
  auth: inherit
}

vars:post-response {
  : 
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
}

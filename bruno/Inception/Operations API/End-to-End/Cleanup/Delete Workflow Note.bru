meta {
  name: Delete Workflow Note
  type: http
  seq: 1
}

delete {
  url: {{baseURL}}/api/operations/workflows/{{workflowId}}/notes/{{workflowNoteId}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 204
}

settings {
  encodeUrl: true
}

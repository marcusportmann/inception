meta {
  name: Delete Document Note
  type: http
  seq: 2
}

delete {
  url: {{baseURL}}/api/operations/documents/{{documentId}}/notes/{{documentNoteId}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
}

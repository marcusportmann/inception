meta {
  name: Finalize Workflow
  type: http
  seq: 15
}

post {
  url: {{baseURL}}/api/operations/finalize-workflow
  body: json
  auth: inherit
}

body:json {
  {
    "workflowId": "{{workflowId}}",
    "status": "completed"
  }
}

settings {
  encodeUrl: true
}
